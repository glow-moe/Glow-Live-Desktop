# Secrets: Discord app ids injected at build time
.appids

# Code-signing config (Azure Trusted Signing account/profile) — see .signing.example
.signing

# Build output (ship binaries as GitHub Releases, not in the tree)
/dist/

# Local
*.log

# Local dev-build counter (scripts/build.sh bumps it)
DEV
